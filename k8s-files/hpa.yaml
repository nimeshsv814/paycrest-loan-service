apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: loan-service-hpa
  namespace: pc-app
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: loan-deploy
  minReplicas: 2
  maxReplicas: 4